name: adhyayan
description: "অধ্যয়ন অ্যাপ"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.0.0
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
